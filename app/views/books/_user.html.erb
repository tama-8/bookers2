
    <div class = 'col-md-3'>
      <h2>User info</h2>
      <table class ='table'>
        <tr>
      <%= image_tag user.get_profile_image(100,100) %>
       </tr>
       <tr>
         <th>name</th>
         <th><%= user.name %></th>
       </tr>
       <tr>
         <th>introduction</th>
         <th><%= user.introduction %></th>
       </tr>
      </table>
      <div class = 'row'>
        <!--編集リンク-->
         <%= link_to edit_user_path(user), class: "btn btn-outline-secondary btn-block" do %>
            <div class="box">
              <i class="fas fa-user-cog"></i>
            </div>
          <% end %>
      </div>
        <h2 class ="mt-3">New book</h2>
        <!--投稿時エラー-->
        <% if book.errors.any? %>
         <div id="error_explanation">
           <h2 id="error_title"><%= book.errors.count %> errors prohibited this book from being saved:</h2>
           <ul>
             <% book.errors.full_messages.each do |message|%>
             <li><%= message %></li>
             <% end %>
           </ul>
          </div>
           <!--エラーメッセージを'/book'で表示-->


         <% end %>
        <!--新規投稿フォーム-->
         <fome action="/books" accept-aharset="UFT-8"method="post">
        <%= form_with model:book do |f| %>
          <div class="fome-grop">
            <label for="title">Title</label>
            <%= f.text_field :title,class:"form-control",placdhlder:"Title"%>
          </div>

          <div class="fome-grop">
            <label for="Opinion">Opinion</label>
             <%= f.text_area :body,class:"form-control",placdhlder:"Opinion"%>
          </div>

          <div class="fome-grop">
            <%= f.submit 'Create Book' , class:"btn bg-success btn-info mt-4"%>
          </div>
        <% end %>
         <!-- 新しいフォームの内容を追加 -->
         <%#= form_for @model do |f| %>
         <%= form_with model:book do |f| %>
         <div class="fome-grop">
            <label for="title">Title</label>
            <%= f.text_field :title,class:"form-control",placdhlder:"Title"%>
          </div>

          <div class="fome-grop">
            <label for="Opinion">Opinion</label>
             <%= f.text_area :body,class:"form-control",placdhlder:"Opinion"%>
          </div>

          <div class="fome-grop">
            <%= f.submit 'Create Book' , class:"btn bg-success btn-info mt-4"%>
          </div>

          <% end %>
        </fome>
        </div>
